script(src='/js/creator/update_mission.js')
#updateMission
  h1 Update Missions
  table.table.table-sm.table-striped
    thead
      tr
        th ID
        th Name
        th 評価
        th
    tbody
      tr(v-for='m in missions')
        td(v-text='m.id')
        td
          a(href='#', v-text='m.name', @click='update(m)')
        td(v-text='m.rate')
        td
          button.btn.btn-danger.btn-xs(type='button', v-if='m.rate == 0', @click='del(m)') Delete

  p 削除できるmissionは1度もプレイされていないmissionのみです

