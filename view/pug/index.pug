doctype html
html(lang=ja)
  head
    include template/_header
    title Train Stamp Rally
    script(defer, src='/js/creator/histories.js')
  body
    include template/_navbar
  .container(role=main)
    .jumbotron
      h1.display-2 Train Stamp Rally
      p 列車に乗って駅をめぐる
      p 時間・距離・金額を競う
      a.btn.btn-primary.btn-lg(href='/auth/account.html') アカウント登録
      a.btn.btn-primary.btn-lg(href='/readme/index.html') 説明書
      a.btn.btn-primary.btn-lg(href='https://gitter.im/ponkotuy/train-stamp-rally') 開発室(Gitter)
    p ※現在位置情報は使わないゲームです
    #histories
      h3 更新履歴
      dl.row
        template(v-for='history in histories')
          dt.col-2.text-right(v-text='format(history.created)')
          dd.col-10(v-text='history.message')
